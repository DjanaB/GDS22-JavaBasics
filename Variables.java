public class Variables {

    // Erstellung des Einstiegspunkts

    public static void main(String[] args) {
        // Variablen Deklaration
        String firstName;
        int x, y, z;


        // Initialisierung
        firstName = "Daniel";

        x = 1;
        y = 2;
        z = 3;

        // Deklaration + Init
        char a = 'a';
        int p = 2, q = 3;

        // variablen Wert tauschen
        firstName = "Max";

        // Die Zahl 1 ist so kein Wert - immer noch ein String
        firstName = "1";

        var secondName = firstName;
        var letter = a;
        var number = p;

        var doubleNr = 4000;


    }
}
